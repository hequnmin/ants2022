import React from "react";
import { Typography } from "@mui/material";

export const Demo = (props) => {
    return(
        <>
            <Typography variant="h4">Demo Page.</Typography>
            <Typography variant="h4">Demo Page.</Typography>
            <Typography variant="h4">Demo Page.</Typography>
        </>
    )
}